LpEsp prueba;

Variables:
    var entero a, a1, b1, c1, d1, entero1, entero2;
    var flotante b, c, d;
    var texto e;
    var logico f;
        
Funciones:
    entero funcion1(entero parametro1, texto parametro2, logico parametro3):

        Variables:
        var entero func1var1, func1var2;
        var flotante func1var3;

        Bloque:


        Regresa

    flotante funcion2(flotante parametro1):

        Variables:
        var entero func2var1, func2var2;
        var flotante func2var3;


        Bloque:

        Regresa


    flotante funcion3():

        Variables:

        Bloque:

        Regresa

Esp:
    imprime('Hola mundo', 5 + 5);
    b = 1 + 2 * 3 / 4 - 5;
    e = 'Adios';
    imprime(e);
    f = a + b * c < d;
    f = falso;

    si(a1 + b1 > c1 * d1):
        a1 = b1 + d1;
    sino:
        a1 = d1 - c1;
    fin;
    b1 = a1 * c1 + d1;
Pse;